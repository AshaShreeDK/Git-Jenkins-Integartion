pipeline
{
agent any 
  stages{
    stage('working with variables') {
      steps {
        script {
          idno = 1
          course = "Jenkins Scripting"
          println "my ID number is ${idno},my current Course is: ${course}"
        }
      }
    }
  }
}
